# Migration `20200917213028-create-data-migrations`

This migration has been generated by t0nylombardi at 9/17/2020, 5:30:28 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Post" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "title" TEXT NOT NULL,
    "body" TEXT NOT NULL,
    "publishedAt" TEXT NOT NULL,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
)

CREATE TABLE "Image" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "title" TEXT NOT NULL,
    "url" TEXT NOT NULL,
    "postId" INTEGER NOT NULL,

    FOREIGN KEY ("postId") REFERENCES "Post"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE TABLE "Tag" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL,
    "freq" INTEGER NOT NULL,
    "postId" INTEGER NOT NULL,

    FOREIGN KEY ("postId") REFERENCES "Post"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE TABLE "RW_DataMigration" (
    "version" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "startedAt" DATETIME NOT NULL,
    "finishedAt" DATETIME NOT NULL,
PRIMARY KEY ("version")
)

CREATE UNIQUE INDEX "Post.title_unique" ON "Post"("title")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200917213028-create-data-migrations
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,44 @@
+datasource DS {
+  // optionally set multiple providers
+  // example: provider = ["sqlite", "postgresql"]
+  provider = "sqlite"
+  url = "***"
+}
+
+generator client {
+  provider      = "prisma-client-js"
+  binaryTargets = "native"
+}
+
+model Post {
+  id          Int      @id @default(autoincrement())
+  title       String   @unique
+  body        String
+  images      Image[]
+  tags        Tag[]
+  publishedAt String
+  createdAt   DateTime @default(now())
+}
+
+model Image {
+  id     Int    @default(autoincrement()) @id
+  title  String
+  url    String
+  post   Post   @relation(fields: [postId], references: [id])
+  postId Int
+}
+
+model Tag {
+  id     Int    @default(autoincrement()) @id
+  name   String
+  freq   Int
+  post   Post   @relation(fields: [postId], references: [id])
+  postId Int
+}
+
+model RW_DataMigration {
+  version    String   @id
+  name       String
+  startedAt  DateTime
+  finishedAt DateTime
+}
```


